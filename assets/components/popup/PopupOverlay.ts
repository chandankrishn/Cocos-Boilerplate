import { _decorator, Component, Node } from "cc";
const { ccclass, property } = _decorator;

@ccclass("PopupOverlay")
export class PopupOverlay extends Component {
  start() {}

  private openPopup() {
    this.scheduleOnce;
  }

  update(deltaTime: number) {}
}
